rule_files:
  - /etc/prometheus/alerts.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:
  - job_name: 'byzantium'
    bearer_token: '${BYZ_METRICS_TOKEN}'
    static_configs:
      - targets: ['gateway:8080']
